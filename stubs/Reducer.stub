const initialState = {

};

const FileName = function(state = initialState,action) {
    switch (action.type) {
        case '':
            return null
        default:
            return null;
    }
};

export default FileName;